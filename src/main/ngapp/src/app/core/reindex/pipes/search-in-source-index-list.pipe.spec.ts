import { SearchInSourceIndexListPipe } from './search-in-source-index-list.pipe';

describe('SearchInSourceIndexListPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchInSourceIndexListPipe();
    expect(pipe).toBeTruthy();
  });
});
