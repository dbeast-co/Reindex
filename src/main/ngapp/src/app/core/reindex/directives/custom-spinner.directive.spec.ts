import {CustomSpinnerDirective} from './custom-spinner.directive';

describe('CustomSpinnerDirective', () => {
  it('should create an instance', () => {
    // const directive = new CustomSpinnerDirective();
    // expect(directive).toBeTruthy();
  });
});
