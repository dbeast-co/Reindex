import { DisableEnterDirective } from './disable-enter.directive';

describe('DisableEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableEnterDirective();
    expect(directive).toBeTruthy();
  });
});
